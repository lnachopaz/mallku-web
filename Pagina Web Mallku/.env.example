# =====================================================================
# MALLKU · Claves de Supabase (login de clientes + base de datos)
# ---------------------------------------------------------------------
# 1. Copiá este archivo y renombralo a:  .env.local
# 2. Completá las dos claves con los valores de tu proyecto de Supabase
#    (Dashboard → Settings → API). Ver CONFIGURAR-SUPABASE.md
# 3. El archivo .env.local NO se sube a git (ya está ignorado).
# =====================================================================

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
